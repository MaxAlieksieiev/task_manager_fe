import styled from 'styled-components';
import {Input as AndInput} from 'antd';


export const Input = styled(AndInput.Password)``;
export const Wrapper = styled.div`
  
`;

export const Label = styled.div`
  margin-bottom: 5px;
  font-weigth: 600;
`;
